name: Build Protobot-Rebuilt

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build-windows:
    name: Build Windows EXE
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-Windows-${{ hashFiles('Assets/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-Windows-

      # This fixes permission issues that often cause crashes on Linux runners
      - name: Fix Permissions
        run: sudo chown -R $USER:$USER .

      - name: Build Unity Project
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: '${{ secrets.UNITY_PASSWORD }}'
        with:
          targetPlatform: StandaloneWindows64
          projectPath: .
          buildMethod: BuildManager.BuildWindowsEXE
          unityVersion: 2021.3.5f1
          # -nographics prevents crashes on headless runners
          # -no-out-of-process-build can stabilize memory management in 2021.3 LTS
          customParameters: -nographics -no-out-of-process-build

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Protobot-Rebuilt-Windows
          path: Builds/
          retention-days: 30
